export enum AppRoute {
  Home = '/',
  Offers = '/catalog',
  Offer = '/offers/:id',
  Cart = '/cart'
}

export enum APIRoute {
  Offers = 'Offers',
  Offer = '/Offers/:id',
}

export enum LS {
  CART = 'cart',
}