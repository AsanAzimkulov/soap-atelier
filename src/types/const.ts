export enum AppRoute {
  Home = '/',
  Offers = '/catalog',
  Offer = '/offers/:id',
  Cart = '/cart',
  Reviews = '/reviews',
  Checkout = '/checkout'
}

export enum APIRoute {
  Offers = 'Offers',
  Offer = '/Offers/:id',
  Reviews = '/Reviews'
}

export enum LS {
  CART = 'cart',
}